// Test file without tab characters for validation testing
#include <stdio.h>

int main() {
    int x = 5;    // This line uses spaces only
    printf("Hello World\n");    // This line also uses spaces
    return 0;
}